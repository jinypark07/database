/* 오라클에서 쓰레기통 기능을 사용하지 않으면 사용할 수 없음. 해당 쓰레기통을 유지하는 기간동안의 데이터만 검색 가능 */
SELECT * FROM 테이블명 AS OF TIMESTAMP(SYSTIMESTAMP -INTERVAL '1'HOUR);
SELECT * FROM 테이블명 AS OF TIMESTAMP(SYSTIMESTAMP -INTERVAL '1'MINUTE);
SELECT * FROM 테이블명 AS OF TIMESTAMP TO_TIMESTAMP('202407241650', 'YYYYMMDDHH24MI');

SELECT OBJECT_NAME,
       DBMS_METADATA.GET_DDL('TABLE', OBJECT_NAME) AS SCRIPT,
  FROM USER_OBJECTS
WHERE OBJECT_TYPE = 'TABLE'
  AND TO_CHAR(LAST_DDL_TIME, 'YYYYMMDD') >= '20210705'
  AND OBJECT_NAME NOT LIKE '%제외할 테이블이 있다면 테이블명%';
